spring.application.name=SchoolManagementSystem

#Server
server.port = 8080

#DataSource
spring.datasource.url=jdbc:mysql://localhost:3306/sms_db?useSSl=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=topik712
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#JPA / Hibernate
#ddl-auto=update ensures tables are created automatically based on your entities.
spring.jpa.hibernate.ddl-auto=update

#show-sql=true prints queries in console, useful for learning.
spring.jpa.show-sql=true


spring.jpa.properties.hibernate.format_sql=true

# Logging (optional but can help)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE


